# Edge-computing-project-


## Setup

1. Clone the repository
2. Copy `wifi_credentials.example.json` to `wifi_credentials.json`
3. Fill in your own credentials in `wifi_credentials.json`

> **Note:** `wifi_credentials.json` is gitignored and must be created locally — it is not included in the repository.