[project]
name = "edge-computing-project"
version = "0.1.0"
description = "Smart Grow: IoT edge telemetry pipeline for real-time plant temperature and environmental monitoring"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "paho-mqtt>=2.1.0",
    "psycopg2-binary>=2.9.13",
    "python-dotenv>=1.2.3",
]
[tool.uv]
package = false
